[project]
name = "fast-shared"
version = "0.1.0"
description = "Shared components for Fast Platform (Schemas, Security, Logic)"
dependencies = [
    "pydantic",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared"]
